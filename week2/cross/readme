第二周作业——十字架：
布局基本完成，阳光照射菊花～
1.在菊花垂直居中时候，只会用top，还有其他方法吗？比如用vertical该怎么实现？

修改：
1. 一上来整个box由于父元素浏览器大小未知，采用绝对定位％形式居中，由于box大小已知，再往左上回移即可。
2. 中间菊花垂直居中方法有二：
    1)设为block，绝对定位，四角设为0，margin自动auto；
    2)设为内敛元素，使用vertical；text－align(这俩大多是针对内敛的啊)，由于是一行，父元素需要制定line－wight＝height。